apiVersion: apps/v1
kind: Deployment
metadata:
  name: diabetes-predictor
spec:
  replicas: 2
  selector:
    matchLabels:
      app: diabetes-predictor
  template:
    metadata:
      labels:
        app: diabetes-predictor
    spec:
      containers:
        - name: diabetes-predictor
          image: tamirasianne/diabetes-predictor:latest
          ports:
            - containerPort: 5000
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"

---
apiVersion: v1
kind: Service
metadata:
  name: diabetes-predictor-service
spec:
  type: NodePort
  selector:
    app: diabetes-predictor
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007